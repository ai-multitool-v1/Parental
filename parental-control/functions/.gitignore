# Compiled Cloud Functions output (anchored: /lib = tsc outDir; do NOT let
# this swallow src/lib — that directory is REAL source code)
/lib/
node_modules/
*.log
.env
# Service account keys must NEVER be committed
*-firebase-adminsdk-*.json
serviceAccountKey.json
