# Build artifacts
.gradle/
build/
local.properties
*.apk
*.aab

# Firebase Android client config is public-by-design (API key ships inside
# every APK) — the real file IS tracked so CI builds connect to Firebase.

# IDE
.idea/
*.iml
.DS_Store
